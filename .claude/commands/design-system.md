---
description: Design system: token architecture, component specs, slides
argument-hint: <design tokens / component spec request>
---

Invoke the `design-system` skill to handle the following request. Apply the skill's full workflow (search its database, follow its checklists, and produce the deliverable).

Request:
$ARGUMENTS
