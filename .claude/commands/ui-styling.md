---
description: shadcn/ui + Tailwind accessible UI implementation
argument-hint: <UI component / shadcn / Tailwind request>
---

Invoke the `ui-styling` skill to handle the following request. Apply the skill's full workflow (search its database, follow its checklists, and produce the deliverable).

Request:
$ARGUMENTS
